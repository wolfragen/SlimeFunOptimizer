# --- Plugin jars (NOT redistributed) -----------------------------------------
# Drop your own Slimefun + addon .jar files in plugins/ and run `python build_data.py`
# to rebuild data/ for your exact versions. The jars themselves are never committed.
plugins/*.jar

# --- Build diagnostic (regenerated by build_data.py) --------------------------
data/missing_recipes.json

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# --- Editors / OS ------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Local agent / scratch ---------------------------------------------------
.claude/
.serena/
NEXT_TASK_*.md
